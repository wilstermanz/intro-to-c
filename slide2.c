/* My First C Program */
#include <stdio.h>
/* Get the input/output functions included in the standard library */

/**
 * Define a function named 'main' that receives no argument values
 * and returns some integer
*/
int main(void)
{
	printf("hello, world\n");

	return (0);
}
	/**
	 * Call the library function 'printf' to print a sequence
	 * of characters i.e. a 'string'
	*/

	/* Return 0 */


/* gcc -Wall -Werror -Wextra -pedantic -std=gnu89 slide2.c */

/*
Discussion:
	- What are the parts of a function declaration?
	- What makes 'main' special?
	- Why does 'main' return 0? What happens if we return
	  something else?
	- What is '\n'?
	- Compilation
*/
